# this script is run after bootstrap and after a new user is configured based on user data, etc
#
# 1: remove the /var/local/packer directory
# 2: remove packer user from /etc/passwd and /etc/sudoers
#

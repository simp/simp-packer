#========================================================================
# simp config answers
#
# Generated for simp scenario on 2017-05-23 20:31:37
# using simp-cli version 4.0.0
#------------------------------------------------------------------------
# You can use these answers to quickly configure subsequent
# simp installations by running the command:
#
#   simp config -A /PATH/TO/THIS/FILE
#
# simp config will prompt for any missing items.
#
# NOTE: All YAML keys that begin with 'cli::' are used
# by simp config, internally, and are not Puppet hieradata.
#========================================================================


---
cli::network::interface: eth0
cli::network::set_up_nic: true
cli::use_internet_simp_yum_repos: true
simp::server::allow_simp_user: false
svckill::mode: warning
cli::network::dhcp: dhcp
cli::network::gateway: 172.31.0.1
cli::network::hostname: PLACEHOLDER_HOSTNAME
cli::network::interface: eth0
cli::network::ipaddress: PLACEHOLDER_IP
cli::network::netmask: 255.255.240.0
simp_options::dns::search:
- PLACEHOLDER_DNS
simp_options::fips: false
simp_options::ldap::base_dn: dc=PLACEHOLDER_LDAP,dc=compute,dc=internal
simp_options::puppet::ca: PLACEHOLDER_HOSTNAME
simp_options::puppet::server: PLACEHOLDER_HOSTNAME

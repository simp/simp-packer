--log-format="%{path}:%{line}:%{check}:%{KIND}:%{message}"
--relative
--no-class_inherits_from_params_class-check
--no-140chars-check
--no-empty_string-check
--no-trailing_comma-check
#  Some of the files are copied to other directories so they are
# not in the correct directories and autoloader fails.
--no-autoloader_layout-check
# This is here because the code can't handle lookups in parameters and we have
# a LOT of those
--no-parameter_order-check
